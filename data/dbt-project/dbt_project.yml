name: dbt_project
version: 1.9.4
config-version: 2
profile: dbt_project
model-paths:
  - models
analysis-paths:
  - analyses
test-paths:
  - tests
seed-paths:
  - seeds
macro-paths:
  - macros
snapshot-paths:
  - snapshots
clean-targets:
  - target
  - dbt_packages
models:
  dbt_project:
    +materialized: table
target-path: target
